name: 'Thank You Act'
description: 'Say "Thank You" to new pull requests'
author: 'Vinicius'

inputs:
  GITHUB_TOKEN:
    description: 'GitHub token'
    requered: true
  TENOR_TOKEN:
    description: 'Tenor API token'
    required: true

runs:
  using: 'node12'
  main: 'dist/index.js'